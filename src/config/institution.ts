/**
 * The institution this deployment serves. Example values until system configuration (X-06, FR-108) stores them;
 * read them only from here so that change touches one file.
 */
export const INSTITUTION = {
  name: "Khanya Skills Institute",
  helpEmail: "help@example.org",
} as const;
